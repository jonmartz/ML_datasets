from setuptools import find_packages, setup

setup(
      name="ML_datasets",
      version="1.0.0",
      description="H2O AutoML Pipeline",
      platforms=["any"],
      packages=find_packages(),
)
